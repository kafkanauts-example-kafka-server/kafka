import React, { useState, useEffect } from 'react';

const App () => {
  const [ID, setID] = useState(0);
  
  useEffect(() => {
    const id = cookies.get('ssid');
    if (id) {
      
    }
  })
}

export default App; 